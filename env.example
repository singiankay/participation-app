# Environment variables template
# Copy this file to .env.local for local development

# Local Database (for development)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/participation_app
DIRECT_URL=postgresql://postgres:postgres@localhost:5432/participation_app

# Production Database (managed by GitHub Actions + Terraform)
# NEON_API_KEY is set in GitHub Secrets and passed to Terraform

# Vercel Deployment (managed by GitHub Actions)
# VERCEL_TOKEN is set in GitHub Secrets
# DATABASE_URL and DIRECT_URL are set automatically by Terraform

# CORS Configuration (optional - defaults to participation-app.vercel.app)
# ALLOWED_ORIGINS=https://participation-app.vercel.app,https://yourdomain.com

# API Authentication (for production)
# API_KEYS=your-api-key-1,your-api-key-2,your-api-key-3